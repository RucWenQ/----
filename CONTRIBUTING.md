# 贡献与维护规范

本项目是面向社会心理学研究生的**科研方法手册**，重点是研究设计、统计方法、科研工具、可视化和论文写作。它不承担系统整理社会心理学理论的任务。内容应帮助读者完成真实科研任务，而不是追求主题数量。

## 一、如何添加 / 修改一节内容

### 修改已有小节

直接编辑对应的 `.md` 文件。例如 1.1 文献检索 → `docs/1-tools/literature-search.md`。

### 新增小节

1. 在对应章节文件夹下新建 `.md` 文件，文件名用 **kebab-case**（小写英文 + 短横线），例如 `effect-size.md`。
2. 文件顶部必须有 frontmatter：
   ```yaml
   ---
   title: 3.0.3 效应量
   description: 一句话描述本节内容
   ---
   ```
3. 在 `docs/.vitepress/sidebar.mts` 对应章节里增加一行：
   ```ts
   { text: '3.0.3 效应量', link: '/3-statistics/effect-size' }
   ```
4. 保存后 `npm run dev` 自动热更新。

### 新增整章

1. 在 `docs/` 下新建 `8-xxx/` 文件夹（数字前缀连续）。
2. 必须包含 `index.md`（章节首页）。
3. 在 `sidebar.mts` 里增加 `'/8-xxx/': [...]` 配置块。
4. 在 `config.mts` 的 `nav` 里增加导航项。

## 二、命名规范

| 类型         | 规则                                  | 示例                                            |
| ------------ | ------------------------------------- | ----------------------------------------------- |
| 章节文件夹   | `数字-英文短名/`                      | `3-statistics/`                                 |
| 小节 md 文件 | `kebab-case.md`                       | `mediation-moderation.md`                       |
| 图片文件     | `kebab-case.{png,svg,jpg}`            | `power-curve.png`                               |
| 资源文件夹   | `docs/public/images/chapters/{章节}/` | `images/chapters/3-statistics/`                 |
| 引用图片路径 | 绝对路径，不带 `public/`              | `/images/chapters/3-statistics/power-curve.png` |
| 内部链接     | 不带 `.md`，相对当前路径              | `[功效分析](./sampling-power)`                  |

## 三、写作规范

### Frontmatter 必填字段

```yaml
---
title: 章节号 + 中文标题 # 例：3.0 基础概念
description: 一句话描述
---
```

### 推荐结构（小节级别）

```markdown
# 章节号 标题

::: tip 本节目标
读完本节读者能做什么。
:::

## 大块 1

正文...

## 大块 2

正文...

## 资源与工具

<ResourceGrid>...</ResourceGrid>

## 延伸阅读

- 链接 / 文献
```

### 强调与样式

- **重要术语**：第一次出现用粗体 `**term**`
- **关键词高亮**：用 `<span class="kw">关键词</span>`（黄色背景）
- **行内代码**：`` `code` ``
- **数学公式**：`$d = (\bar{X}_1 - \bar{X}_2) / s$` 或 `$$...$$`

### 图片

引用方式：

```markdown
![功效曲线](/images/chapters/3-statistics/power-curve.png)
```

文件放在：`docs/public/images/chapters/3-statistics/power-curve.png`

## 四、组件速查

详见 [vibe coding 提示词](./PROMPT.md) 第八节。

## 五、事实核查与引用规范

### 1. 资料来源优先级

事实性或方法性结论应优先依据：

1. 原始方法论文、报告规范或学术组织发布的指南；
2. 统计教材、方法学专著和同行评议综述；
3. 软件或工具的官方文档；
4. 高质量大学课程资料，作为解释性补充。

不要把搜索摘要、营销页面、论坛回答或 AI 输出本身当作证据。引用文献前应核对作者、年份、题名、版本、期刊或出版社；有 DOI 时优先附 DOI，软件操作则链接官方文档并注意版本差异。

### 2. 统计内容的最低要求

每个统计方法页面应说明：

- 它回答什么研究问题，不回答什么问题；
- 变量类型、观测单位和独立 / 配对 / 嵌套等数据结构；
- 关键假设、适用边界，以及假设不满足时的处理方向；
- 需要报告的估计值、效应量、不确定性和模型诊断，而不只报告 `p` 值；
- 示例数据的来源。虚构或模拟数据必须明确标为“教学示例”或“模拟数据”；
- 示例中的均值、差值、统计量、自由度、`p` 值、效应量和置信区间应彼此一致。能运行的代码应实际运行核对，不能凭文字猜测输出。

不要使用“完全等价”“一定稳健”“只要显著就证明”等绝对表述，除非紧接着写清成立条件。横断或观察性研究默认使用“关联”“预测”等语言；中介系数、回归系数和显著性本身不能证明因果机制。

### 3. 时效性内容

期刊指标、软件菜单、插件功能、投稿规则和在线服务会变化。写入时应标明适用版本或核查日期；无法确认的内容用 `::: info 待核查` 标注，不要把暂时信息写成长期规则。

## 六、提交前检查

每次修改完成后，至少检查：

1. 修改范围是否只覆盖本次指定的小节或同一主题；
2. Frontmatter、标题编号、sidebar 文案和文件路径是否一致；
3. 所有内部链接目标是否存在，路径不带 `.md`；公共资源是否位于 `docs/public/`；
4. 新增或修改的引用是否真实、可追溯，标题和年份是否准确；
5. 示例数值、代码输出、图表和正文结论是否相互一致；
6. 是否把统计关联误写成因果关系，或省略了方法成立的关键条件；
7. 运行 `npm run build`，确认 VitePress 构建通过且没有死链或 Markdown / Vue 语法错误。

若当前环境不能运行构建，应在交付说明中明确写出原因，不能声称已经验证。

## 七、分批维护原则

- 一次事实核查聚焦**一个小节或一个紧密主题组**，例如“t 检验 + 差异分析总览”，不要一次重写整章。
- 修正文档时保留原有教学结构和语气；事实错误直接改，存在学术争议的内容应呈现条件和边界。
- 链接、标题、术语等确定性问题可以随当前批次一并修复。
- 完成后列出：核查页面、主要事实修正、引用来源、新增或仍待处理的问题、验证结果。
